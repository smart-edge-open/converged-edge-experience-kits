---

- debug:
    msg: "Using git token for repository checkout"
  when: git_repo_token != ""

- name: Checkout main repository
  git:
    repo: "{{ git_repo_url }}"
    dest: "{{ git_repo_dest }}"
    version: "{{ git_repo_branch }}"
    update: no
